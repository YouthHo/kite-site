# 关系图谱 / 名场面 / 势力架构 —— 工程缺陷根治 Prompt

> 纯工程修复任务，**不涉及任何奖项申报素材**（静帧/视频/checklist/跑分等一律不做）。
> 生效基准：仓库 HEAD = `57ba69f`。执行前 Agent **必须**先 `git log --oneline -5` 与 `git status` 确认 HEAD 与上述文件一致，禁止凭旧记忆动手。
> 所有改动均为**手术式编辑**：保留现有全部功能（档案巡览 10 幕、解密模式、路径模式、聚焦隔离、mini-map、力导向、时间轴演化、键盘导航、reduced-motion 降级）。

---

## §1 不可违背的铁律

1. **纯静态**：不引入后端 / SSR / 运行时外部 API。
2. **三平台部署不变**：`.github/workflows/` 三条 CI + `npm run build` / `npm run build:gh` 双模式必须保持通过。
3. **双主题 + reduced-motion 不变**：`data-theme` dark/light、`prefers-reduced-motion` 降级必须保留且所有新增 UI 在两种主题下都清晰可读。
4. **版权零风险**：名场面配图只能使用公版/原创资产，禁止剧照或受版权图片；找不到合适图就保留占位，不要用侵权图替换。
5. **性能预算**：不新增首屏阻塞；图谱渲染保持 rAF 常驻/按需渲染结构。
6. **不破坏既有交互**：除本 prompt 明确指出的行为变更外，巡览/解密/路径/隔离/mini-map/力导向/时间轴/键盘导航全部保持原样。

---

## §2 当前真实状态（已完成 · 禁止推倒重写）

- `src/views/GraphView.vue`（图谱页，含顶栏状态条、右侧人物列表、工具条、巡览旁白、悬停电报条、档案卡）
- `src/graph/GraphEngine.js`（自研 Canvas2D 引擎，节点/边/标签/高亮/命中/相机渲染）
- `src/components/GraphStatusBar.vue`（顶部 MODE 状态条）、`GraphToolbar.vue`（右侧模式工具条）、`GraphHelpPanel.vue`（帮助面板）、`GraphOnboarding.vue`（首引导）
- `src/views/ScenesView.vue`（名场面页）、`src/views/ArchitectureView.vue`（势力架构树）
- `src/utils/anim.js`（`pageEnter` / `typewriter` 等）
- `src/styles/main.scss`（含 `.k-card` / `.archive-tape` / 主题变量；注意 line 140 注释：archive-tape 的条纹装饰**已移除**）

---

## §3 逐条问题与根治方案（带 file:line 实锤）

### A. 关系图谱顶部 MODE 组件与右侧标签重叠
- **现象**：顶部居中的状态条与右上角工具条在窄/中屏相互压字。
- **根因**：`GraphView.vue:407-415` 状态条 `class="absolute top-3 left-1/2 -translate-x-1/2 ... max-w-[92%]"` 几乎占满宽度；`GraphView.vue:416-429` 工具条 `absolute top-3 right-3 max-w-[62%]`。两者在大部分视口下水平区间重叠。
- **修复**：
  - 改为**左状态条 + 右工具条**双锚点且不越界：状态条改用 `left-3`（非居中），并约束其最大宽度（如 `max-w-[calc(100%-Xrem)]` 或 `max-w-[42%]`），确保右端不进入工具条左端。
  - 工具条本身 `flex-wrap justify-end`，在窄屏会换行，需保证换行后也不与状态条在垂直方向重叠（可给状态条更高的 `z` 或限制其宽度）。
  - 响应式验收：在 1280 / 768 / 375 三档宽度下，状态条文案与工具条按钮均**互不压字**。

### B. 右侧人物列表：点击名字 = 勾选/取消；感叹号 ⓘ = 查看档案详情
- **当前行为（需改）**：`GraphView.vue:229-237` `onRowClick` 直接 `openPanel(n.id)`（点名字=看档案）；`GraphView.vue:238-243` `togglePerson` 仅由行内复选框 `@click.stop` 触发；`GraphView.vue:584-591` 的 ⓘ 按钮也调 `openPanel`。
- **目标行为**：
  - 点击**名字行本体** → 切换该人物的「筛显/隐藏」勾选（`togglePerson(n.id)`），**不再**直接打开档案。
  - 行内 **ⓘ 按钮** → 打开该人物档案详情（`openPanel(n.id)`）。
  - 复选框（方形勾选）保持现有 `togglePerson` 行为不变（点名字与点方框等价，均为筛选）。
- **提示文字同步修改**：
  - `GraphView.vue:562`：`'点击名字 = 查看档案 · 勾选 = 筛显图谱'` → 改为 `'点击名字 = 勾选/取消筛显 · 点击 ⓘ = 查看档案详情'`。
  - `GraphHelpPanel.vue` 若提及侧栏交互，补充一条：『右侧人物列表：点击名字行 = 勾选筛显该人物；点击 ⓘ = 打开档案详情』。
  - 保持 `path` 模式下行点击=选端点逻辑（`GraphView.vue:230-235`）不变。

### C. 悬停高亮：仅圈内触发 + 做成开关 + 关联人物也高亮 + 关系字嵌进线里
这是本任务最复杂的一条，拆为 C1–C4。

**C1 命中范围改为「鼠标中心在圆圈内」**
- 根因：`GraphEngine.js:211-227` `_hitNode` 用固定 `hitR = 26 / ui`（屏幕 26px 命中半径），对小区块节点远大于其视觉半径，导致「靠近就高亮」。
- 修复：命中半径改为基于**节点实际半径**——`hitR = nodeRadius(n) * ui + 容差(约 4px)`（世界单位），即只有当指针中心落在该人物圆圈（含极小容差）内才判定命中。复用 `nodeRadius(n)`（`GraphEngine.js:833`）。

**C2 高亮做成可开关**
- 新增一个**悬停高亮开关**（建议放在 `GraphToolbar.vue` 增加一枚 toggle 按钮，或在 `GraphView` 增加一个 `hoverHighlight` ref，默认 `true`）。
- 开关关闭时：悬停节点**不产生任何高亮/变暗/邻居强调/边电报脉冲**，画面保持静态（仅保留点击等主交互）。
- 开关状态可存 `localStorage`（复用 `src/store/app.js` 的持久化风格），默认开启。

**C3 高亮时关联人物也要高亮**
- 根因：`GraphEngine.js:594` 非邻居节点被 `dimmed`（透明度 0.14），但邻居节点只是「不被 dim」，与常态无视觉区分，用户因此「不知道是谁」。
- 修复：悬停某节点时，对其**邻居节点做正向强调**（如加亮描边/加粗光晕/略增透明度对比），使「谁与之关联」一目了然；当前 hover 节点本身也强调。非邻居仍 dim。强调样式需明显区别于常态（例如邻居节点加金色细环或提高亮度 15–20%）。

**C4 关系文字嵌进关系线（根除标签/线对不上）**
- 根因：`GraphEngine.js:723-760` 在边**中点上方**画药丸标签 + 碰撞避让，标签会偏离曲线，造成「线在这里、字在别处」。
- 修复（推荐方案）：**将关系标签沿曲线绘制**——
  - 在边二次曲线中点处，按该点切线方向 `ctx.save(); ctx.translate(midX, midY); ctx.rotate(tangentAngle);` 后绘制文字（水平于切线），使文字始终贴在线上。
  - 文字底色用半透明圆角底板（`T.labelBg`）保证可读性；仅对**当前悬停节点的邻边**（或 zoom 足够大时全部）绘制，避免全图杂乱。
  - 仅当该边在屏幕上足够长（如像素长度 > 文字宽度 + 余量）才绘制，过短边不画文字（避免重叠）。
  - 删除原 723–760 的「中点上方药丸 + 垂直堆叠避让」逻辑，或改写为沿切线绘制。
- 验收：悬停郑耀先时，其每条关系的文字都贴在该条线上、随线方向倾斜，不再漂浮错位。

### D. 名字写在圆圈内；圈外不写任何文字；字号自适应、不换行不超出
- 根因：`GraphEngine.js:667-719` 把节点名画在圆圈**外**（下/上/右/左/斜位候选 + 引线兜底）。
- 修复：
  - **彻底移除** 667–719 这段「圈外标签」逻辑（含贪心避让与引线）。
  - 改为在**圆圈内部**居中绘制姓名：
    - 字号根据 `r`（节点半径，世界单位 × ui）自适应：`fs = clamp(r * k, minFs, maxFs)`，使名字在圈内尽可能大但留边距（边距 ≈ r*0.18）。
    - **字重**随圆圈大小适配（大圈 `600`、小圈 `500`）。
    - 先 `measureText` 测量，若宽度超过 `2*(r - 边距)` 则逐步缩小 `fs` 直到放得下；若缩到 `minFs` 仍放不下，**则该节点不画名字**（宁可不画，也绝不溢出/换行）。
    - **绝对禁止换行**：单行绘制，`textBaseline='middle'`、`textAlign='center'`，位置为节点圆心。
    - 印章节点（kite/shadow，`GraphEngine.js:624-651`）当前只画 `code` 前 2 字——改为同样画**完整姓名**（与普通人一致），必要时名字 + 代号分行内（仍保证不溢出；放不下则只画名字）。
  - 低 zoom 时小圈自然不画名字（符合「圈外不写、圈内放不下就不写」），zoom 放大后名字自然显现。

### E. 打字机文字看不懂意义 → 换设计；浅色模式下打字机与 MODE 文字不可见
- **E1  redesign 悬停电报条（打字机）**
  - 根因：`GraphView.vue:264-288` `handleHover` 用 `typewriter` 生成「接入 郑耀先 [风筝轴] · 代号「六哥」· 上司·师徒·…」这种一长串电报体，用户认为无意义。
  - 修复（换设计思路）：用**简洁静态信息块**替代逐字打字机——
    - 悬停时底部（或节点旁）显示一个小巧的**非打字机**信息条：仅 `姓名` + `阵营色点` + `代号`（如「郑耀先 · 军统 · 六哥」），关系信息改由 C4 的「线上嵌字」承载，不必在电报条里罗列。
    - 删除 `handleHover` 中的 `typewriter(...)` 调用，改为直接 `textContent =` 静态赋值（保留 `prefersReduced` 分支逻辑可简化）。
    - 档案卡简介 `briefEl` 的打字机（`GraphView.vue:308-312`）可保留或同样改为静态——**保留巡览旁白 `tourEl` 的打字机**（叙事需要），但需保证其在浅色模式可读（见 E2）。
- **E2 浅色模式文字不可见（必须修）**
  - 现象：`GraphStatusBar`、悬停电报条、巡览旁白在 `data-theme=light` 下文字看不见。
  - 根因：这些 DOM 覆盖层用固定浅色文字（`#8a8275` / `#e8dcc8` / `#d8a0a8`）叠在深色半透明底上；但在浅色主题下图谱画布区域背景/对比关系变化，导致低对比消失。
  - 修复（审计 + 主题感知）：
    - 列出并修正所有图谱覆盖层文字颜色，使其**双主题都清晰**：`GraphStatusBar.vue`（line 29/31/35/37/38）、`GraphToolbar.vue`（idle/active 类）、`GraphView.vue` 的悬停电报条(459-462)/巡览旁白(443-452)/解密提示(455-457)/路径提示(432-440)/隔离退出按钮(465-471)。
    - 推荐做法：这些覆盖层使用**自带不透明深色底板**（如 `bg-[#0e0e0e]/90`）+ 浅色文字，使其在任何主题下都可读（图谱区底色本就偏深，叠深色板最稳）；或读取 `theme` 响应式变量切色。
    - Canvas 内的标签/节点文字已通过 `setTheme` 的 `light` 令牌（`GraphEngine.js:77-96`）处理，需**实测浅色模式下单字/关系字是否清晰**，若 `label: rgba(60,52,40,0.95)` 在浅色画布上不够，提高对比。
  - 验收：切换 dark/light 主题，顶部 MODE 条、工具条、悬停信息、巡览旁白、解密提示在两种主题下均**清晰可读、无消失**。

### F. 名场面：加载卡顿时首行三卡显示棕色横线条纹 → 删除该效果
- **现象**：点击名场面页卡顿 1–2 秒期间，首行三张卡片呈现「八九行棕色横线间隔分布」的条纹。
- **根因分析**：`src/data/scenes.json` 所有 `image` 均为 `/images/placeholders/scene.svg`（该占位 SVG 本身是干净深色「名场面」文字块，**并非条纹**）。条纹来自**加载期 `<img loading="lazy">` 尚未取到图时，浏览器显示的破图/占位条纹**（部分浏览器/系统对未加载或加载失败的 img 渲染为条纹状占位）。`ScenesView.vue:66-82` 的 `<img ... loading="lazy">` 是诱因。
- **修复（根治，确保加载期绝不出现条纹）**：
  1. **复现并定位**：用 DevTools Network 限速（如 Slow 3G）模拟卡顿，观察首行卡片在图片加载前的实际渲染元素与 computed style，确认条纹来源（破图占位 vs 某 CSS）。
  2. 给 `<img>` 及其容器**设置纯色背景**：在 `ScenesView.vue:72` 的 `<img>` 上加 `bg-[#121212]`（或 `k-card` 背景），使加载期显示干净深色块而非条纹破图。
  3. **首屏图片改 `eager`**：首行（或整页）图片去掉 `loading="lazy"`，改为立即加载（与路由 chunk 同步），缩短/消除条纹可见窗口。
  4. 若复现发现是某个 SVG/占位资产本身带条纹，则将其替换为**纯色无条纹**占位（scene.svg 已满足，确认它确实是当前所用且非条纹即可）。
  5. 验收：无论网络多慢，名场面卡片在加载期只显示**干净纯色块**，绝不出现棕色横线条纹；图片加载完成后正常显示。

### G. 势力架构：关系树顶部圆圈被上边界切掉（含光晕）
- **根因**：`ArchitectureView.vue:103` `<div class="overflow-x-auto pb-6">` 设了 `overflow-x: auto`；CSS 规范下，当一轴为 `auto/scroll` 而另一轴为 `visible` 时，`visible` 会被强制计算为 `auto`，于是该容器在**垂直方向也成为滚动容器**，把顶层（level 0）圆圈及其 `box-shadow` 光晕（`ArchitectureView.vue:123` `0 0 30px`）裁掉。
- **修复**：
  - 在滚动容器内（或该 `overflow-x-auto` 元素本身）**增加顶部内边距**，使顶层节点 + 其光晕位于内容盒（padding 区不被裁切）内：例如给 `<div class="overflow-x-auto pb-6">` 加 `pt-12`（≈48px），或给内部 `<div class="min-w-[720px]">` 顶部留白。**padding 区域不会被滚动容器裁剪**，光晕即可完整显示。
  - 同时确认：悬停时 `group-hover:scale-110` + `group-hover:shadow-[0_0_28px...]`（`ArchitectureView.vue:119`）放大后的圆圈与光晕也**不被裁切**（padding 需 ≥ 最大光晕半径 + 放大后溢出）。
  - 若仍有裁剪，可把 `overflow-x-auto` 下移到更外层、让树自身所在容器 `overflow: visible`，或改用 `overflow: clip` + `overflow-clip-margin` 给足余量。
  - 验收：顶层圆圈完整显示，鼠标悬停放大 + 光晕放大后**仍不被上边界切割**。

---

## §4 执行路线（每阶段独立 commit，可回滚）

- **Phase 1（A）** 状态条改左锚点 + 限宽，工具条保右锚点，三档宽度验证不重叠。commit `fix(graph): 状态条与工具条不再重叠`
- **Phase 2（B）** 侧栏行点击→togglePerson；ⓘ→openPanel；提示文字 + 帮助面板同步。commit `fix(graph): 侧栏点名为筛选、ⓘ为看档案`
- **Phase 3（C1–C4）** 命中半径改节点半径；新增悬停高亮开关（localStorage）；邻居正向强调；关系字沿曲线嵌入。commit `fix(graph): 悬停高亮仅圈内触发+开关+邻居高亮+关系字嵌线`
- **Phase 4（D）** 移除圈外标签，名字改圆内自适应单行绘制（含印章节点）。commit `fix(graph): 姓名移入圆圈内、自适应不溢出`
- **Phase 5（E）** 悬停信息条去打字机改静态；所有图谱覆盖层双主题可读；巡览旁白保留且浅色可读。commit `fix(graph): 悬停信息改静态+覆盖层双主题可读`
- **Phase 6（F）** 名场面卡片加纯色底 + 首屏图 eager，复现确认无条纹。commit `fix(scenes): 加载期不再显示条纹占位`
- **Phase 7（G）** 势力架构滚动容器加顶部 padding，光晕不被裁。commit `fix(arch): 顶层圆圈与光晕不再被边界裁切`

---

## §5 总体验收（DoD）

1. 顶栏 MODE 状态条与右上工具条在 1280/768/375 宽度下**互不压字**。
2. 右侧列表：点名字行 = 切换筛显；点 ⓘ = 打开档案；提示文字已更新；path 模式行点击仍=选端点。
3. 悬停高亮**仅当指针中心在某人物圆圈内**才触发；有关闭开关且状态持久化；悬停时该节点与**邻居均被正向强调**（非邻居变暗）；每条关系的文字**贴在对应线上、随线倾斜**，郑耀先等密集节点不再错位。
4. 所有节点姓名**绘制在圆圈内部**，圈外无任何文字；字号/字重随圈大小适配；无换行、无溢出（放不下则不画）。
5. 悬停信息为**静态简洁块**（非电报打字机）；图谱全部覆盖层（状态条/工具条/悬停/巡览/解密/路径/隔离）在 **dark 与 light 双主题下均清晰可读**；巡览旁白保留打字机且浅色可读。
6. 名场面页无论网络多慢，加载期**只显示干净纯色块，无棕色横线条纹**。
7. 势力架构顶层圆圈**完整显示**，悬停放大 + 光晕**不被上边界裁切**。
8. 既有功能（巡览 10 幕、解密、路径、隔离、mini-map、力导向、时间轴、键盘导航、reduced-motion）全部正常；`npm run build` 与 `npm run build:gh` 双模式通过；三平台 CI 绿。

---

## §6 起点命令

```bash
cd "D:/autoclaw/workspaces/kite-site"
git log --oneline -5          # 确认 HEAD = 57ba69f
git status --short            # 确认工作区干净
# 从 Phase 1（A 顶栏重叠）开始
```

---

## §7 禁止事项

- **禁止**为奖项/申报做任何事（静帧图、申报视频、checklist、外部跑分报告等）。
- **禁止**重写 `GraphEngine.js` / `GraphView.vue` 整体结构，只做本 prompt 指明的手术式修改。
- **禁止**删除/弱化现有功能：巡览、解密、路径、聚焦隔离、mini-map、力导向、时间轴演化、键盘导航、reduced-motion 降级。
- **禁止**在名场面使用受版权剧照；缺图就保留/改进占位，不得侵权替换。
- **禁止**改变三平台部署与双模式构建。
- **禁止**为「让条纹消失」而隐藏卡片内容导致空白——必须显示干净纯色占位，图片加载后正常呈现。
- 每次改动后必须跑 `npm run build` 与 `npm run build:gh` 双模式验证通过。
