# 2026-08-10

## 接入三平台自动部署（GitHub Pages / Vercel / Cloudflare Pages）

提交：`745982d`（已提交，**未推送** — 本机无 GitHub 凭据）

### 改动
- `.github/workflows/` 新增 3 条工作流，均监听 `master`/`main` push + workflow_dispatch
  - Vercel / Cloudflare 工作流带 guard step：secrets 缺失时优雅跳过，不让 CI 变红
  - Pages 工作流按仓库名自动推导 `VITE_BASE`，识别 `<owner>.github.io` 用户站点走根路径
- `vite.config.js`：`base` 改为读 `process.env.VITE_BASE`（默认 `/`），新增两个构建期插件
  - `spa-fallback-404`：复制 index.html → 404.html，解决 GH Pages 无 rewrite 的深链接 404
  - `rewrite-data-asset-base`：改写 `src/data/*.json` 中硬编码的 `"/images/` 前缀（141 处），
    仅在 base !== '/' 时生效。**这是子路径部署最大的坑**，Vue 模板里的 `<img src>` Vite 会自动处理，
    但 JSON 里的纯字符串不会。
- `scripts/build-gh.cjs` + `npm run build:gh`：跨平台注入 base，本地复现 Pages 构建
- `public/_headers`（Cloudflare/Netlify 缓存）、`public/.nojekyll`、根目录 `DEPLOY.md`

### 环境注意事项
- **vite build 清空 dist 会被沙箱 safe-delete 拦截**（trash 操作失败）。
  规避：构建前先 `rm -rf dist`（需 sandbox 放行）或把 dist 改名移开。
- 本机 `credential.helper=helper-selector`，无 tty 时必然失败；Windows 凭据管理器里
  也没有 github 凭据，`gh` CLI 未安装。推送需用户自行处理或提供 PAT（须含 `workflow` scope）。

### 项目遗留
- `public/sitemap.xml` 域名仍是占位符 `kite.example.com`
- `dist_bak/` 历史构建备份被 Git 追踪，建议清理

### 冲奖优化战略（用户目标：国际设计奖）
- 已输出 `AWARD-OPTIMIZATION.md`：现状诊断 + 按维度优化蓝图 + 自包含 Agent 执行 Prompt（阶段0~5）。
- 核心判断：视觉/质感已达标，短板按杠杆排序为 ①缺 Signature Moment ②性能门槛(ECharts 全量~1MB + 阻塞式 Google Fonts) ③交互未到惊艳 ④首页叙事不够锋利 ⑤细节闭环(OG=picsum占位/sitemap=example.com/图片未优化) ⑥a11y与移动端一致性 ⑦版权风险。
- 3 个必做签名交互：自定义光标+磁吸、主题化页面切换(red线wipe)、首页 WebGL/Canvas 引线/纸屑层。
- 硬约束：不得破坏已接好的三平台部署；保留双主题与 reduced-motion 降级；只用公版/原创资产。

### 教训（重要）
- 写"交给 Agent 执行的 handoff Prompt"前**必须重新 `git log` + 读关键文件对齐 HEAD**，不能照旧快照写。本次 KITE-SITE-REBUILD-PROMPT.md 初版照旧状态写，但仓库在 21:34–22:18 已提交 6 个优化 commit（perf/签名交互/视觉/无障碍），导致 Prompt 把已完成工作当成待办、还指令"新建"已存在的组件。已重写对齐真实状态（commit 5c46f3a 之后）。
- 项目推进极快（多 commit 集中当天完成），任何跨会话的交付物都要先核对当前真实状态。

### PM Brief：从底层到顶层脱胎换骨重建（纯静态 / 全球领先资料站）
- 用户以产品经理视角，要求「纯静态定位不变」，从地基到顶层重建为国际顶尖设计技术水平的风筝资料站。
- 已输出 `KITE-SITE-REBUILD-PROMPT.md`：自包含 Agent 执行 Prompt（角色/北极星/6 条铁律/Layer0~5 蓝图/Phase0~7 路线/DoD/起点）。
- 6 条铁律：①纯静态(禁后端/DB/SSR/CMS) ②三平台部署不变 ③双主题 ④reduced-motion ⑤版权零风险(公版+原创) ⑥性能预算(LCP<2.5/TBT<200/CLS<0.1/首屏JS<200KB/LH≥95)。
- 重点：3 个必做签名交互(自定义光标+磁吸、主题化页面切换、首页 WebGL/Canvas 质感层)；建议 TypeScript + i18n(中英)；ECharts 按需引入。

### 关系图谱专项（用户聚焦：全站核心功能脱胎换骨）
- 当前 `src/views/GraphView.vue`(775行) 已实现：ECharts graph(按需懒加载)、手绘坐标+力导向双模式、BFS最短路径、EP1-46时间轴播放、阵营/关系类型/人物多维筛选、键盘导航、点击电报打字机档案卡、双主题、reduced-motion。天花板：ECharts 节点只能是圆、边只能简单曲线。
- 数据 `src/data/relationships.json`：nodes{id,name,code,faction,x,y}(0-100坐标)、links{source,target,label,type,strength,tone}；rich 字段在 characters.json(episodes/identity/actor/brief)。
- 已输出 `GRAPH-REBUILD-PROMPT.md`：建议替换 ECharts 为 d3-force+Canvas2D/WebGL 自研引擎，做风筝线边/封条节点/解密模式/聚焦抽线/路径电报脉冲/时间演化，Layer0 数据契约 → Layer5 奖项证据。铁律继承全站 6 条。
