# 2026-08-11 工作日志

## 关系图谱交互/界面层重构 Prompt（GRAPH-UX-REBUILD-PROMPT.md）
- 用户反馈：图谱"操作逻辑漏洞多、不直觉、缺乏说明、文字重叠、界面不友好"。
- 先读最新状态再写（吸取 8-10 教训）：HEAD=58f2396，GraphView.vue≈638行、GraphEngine.js≈798行、useGraphData.js、schema.md、CustomCursor/NameBadge/SealStamp、store/app.js、characters.json。
- 根因定位（带 file:line）：
  - 文字重叠：GraphEngine.js 669-677 节点名标签无避让（仅悬停标签 682-719 有避让）；fit 视图 30 节点间距~80px 必压字；力导向破坏手绘 x/y 布局且无恢复通道。
  - 缺乏说明：仅 GraphView.vue 369 一行近黑底弱提示；全仓库无 help/onboarding/legend 说明组件；图例仅色块无语义（大小/印章/虚线）。
  - 操作逻辑：tour/decryptMode/pathMode/focusClick/layoutMode 五开关可叠加、状态隐藏；decrypt 下点击被静默劫持为"揭开"而非看档案；两个"解密"概念混淆；Esc 状态机隐藏(278-287)；侧栏行点击=筛选反直觉、personSel 默认全选致"全高亮"误读。
  - 小 bug：GraphEngine.js 307-308 cam.x 重复赋值。
- 方案：在现有引擎之上重建交互层（非重写引擎）→ Layer0 统一状态机 / Layer1 标签避让+引线+分级显隐 / Layer2 引导+图例 / Layer3 工具条+状态条+主操作恒定 / Layer4 上下文 / Layer5 a11y+奖项素材。Phase0-7 每阶段独立 commit。
- 铁律：纯静态、三平台部署不变、双主题+reduced-motion、版权零风险(禁剧照/演员照)、性能预算、不破坏既有引擎/数据层。

## 工程评分与"提升到 9+" Prompt（ENGINEERING-TO-9-PROMPT.md）
- 用户要求：只聊工程层面；严禁再提奖项申报素材（静帧/视频/checklist/跑分），已写入 ~/.workbuddy/MEMORY.md 长期偏好。
- 工程综合评分 7.5/10。剩余真实工程缺口（已核实 HEAD 58f2396）：
  - A i18n 英文版未做（无 src/i18n）
  - B 字体未自托管（index.html:38-47 仍 Google Fonts）
  - C 占位域名 kite.example.com 残留（index.html:31/35、sitemap.xml、robots.txt:3）
  - D 构建无真实体积门禁（vite.config.js:72 chunkSizeWarningLimit:1500 实为隐藏警告）
  - E 图谱模式切换收尾（mode 枚举已在 GraphView.vue:26 统一 browse|decrypt|path|tour；focusClickId/layoutMode 正交）
  - F 健壮性（i18n fallback / VITE_SITE_URL 注入）
- **纠正两条此前误判**：① 状态机"5开关叠加"在新代码已被 mode 枚举收敛；② echarts 非死依赖——TimelineView.vue:6-10 仍在用，manualChunks.echarts 有效不可删。
- 交付 ENGINEERING-TO-9-PROMPT.md：含角色/北极星/7 条铁律(含"禁奖项素材")/真实状态(已完成+剩余)/A-F 细则(确切 file:line)/Phase0-F/DoD/起点/不要做清单。

## 教训（已多次验证）
- 写 handoff/执行 Prompt 前必须 `git log` + 读真实源码核对 HEAD 与行号，否则会把已完成项当待办、误导 Agent 重做或覆盖。
- 用户明确点过"没看最新状态就写 prompt"——任何跨会话交付物都要先对齐真实状态再动笔。

## 本轮回应用户要求交付 ENGINEERING-TO-9PLUS-PROMPT.md（HEAD=57ba69f）
- 工程 7.5→9+ 的 Agent 执行 prompt；纯工程，不含任何奖项素材（已立铁律）。
- 缺口 A-F：占位域名(VITE_SITE_URL 注入) / 字体自托管 / i18n英文版 / 图谱状态机收敛(focusClick+layoutMode 正交需统一) / 体积门禁 / 依赖审计。
- 写后二次核实纠错：① `robots.txt:3` 也有 kite.example.com 占位 → 补入 Phase A；② `echarts` 非死依赖(TimelineView.vue:6-10,160 在用, manualChunks.echarts 依赖) → Phase F 改为"禁止移除"。已修正落盘。

## 本轮：用户逐条列出 UI bug，交付 GRAPH-UX-FIX-PROMPT.md（HEAD=57ba69f）
- 用户仅要工程修复，严禁奖项素材（已立铁律）。逐条定位 file:line：
  - 图谱①顶栏 MODE 与右工具条重叠：GraphView.vue:407-429（状态条 max-w-92% 居中 + 工具条 right-3 max-w-62% 冲突）→ 改左锚点+限宽。
  - 图谱②侧栏：点名字=看档案(openPanel,229-237) 应改为=筛选(togglePerson)；ⓘ按钮(584-591)改看档案；提示文字(562)同步；help 面板补一条。
  - 图谱③悬停高亮：_hitNode 命中半径固定 26px(211-227)→改节点半径；加开关(localStorage)；邻居也要正向强调(594 仅 dim 非邻居)；关系字嵌进线里(删 723-760 中点药丸，改沿曲线切线绘制)。
  - 图谱④名字移入圆圈内：删 667-719 圈外标签；圆内自适应字号/字重单行不溢出；印章节点(624-651)改画全名。
  - 图谱⑤打字机 redesigh：handleHover 电报条(264-288,459-462)去 typewriter 改静态简洁块；浅色模式覆盖层文字不可见(StatusBar/Toolbar/悬停/巡览/解密)→ 双主题可读审计；巡览旁白保留打字机但浅色可读。
  - 名场面棕色条纹：scenes.json 全用 /images/placeholders/scene.svg(干净深色占位非条纹)；根因=loading=lazy 未取图时浏览器破图条纹占位 → 加纯色底 bg + 首屏图改 eager，复现确认无条纹。
  - 势力架构顶部圆圈被切：ArchitectureView.vue:103 overflow-x-auto 强制 overflow-y=auto 裁掉顶层光晕 → 容器加 pt-12 让 padding 区不被裁。
- 铁律：不破坏既有功能(巡览/解密/路径/隔离/mini-map/力导向/时间轴/键盘/reduced-motion)；名场面禁侵权剧照。
